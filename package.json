{
	"name": "@fnlb-project/database",
	"description": "FNLB Database Client",
	"version": "1.0.0",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"scripts": {
		"build": "bun run tsc && bun build --minify --target=bun --format=esm --outdir=./dist ./src/index.ts ./src/index.ts",
		"lint": "bunx --bun @biomejs/biome check --apply .",
		"prepare": "bun run build"
	},
	"author": "FNLB-Project",
	"files": ["dist"],
	"devDependencies": {
		"@biomejs/biome": "^1.6.0",
		"@types/bun": "^1.0.8",
		"typescript": "^5.4.2"
	}
}
